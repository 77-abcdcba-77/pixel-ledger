{
  "name": "pixel-ledger-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "assets:app": "node scripts/generate-ios-assets.mjs",
    "assets:ios": "npm run assets:app",
    "assets:android": "npm run assets:app",
    "ios:sync": "npm run assets:ios && npm run build && cap sync ios && npm run assets:ios",
    "ios:open": "npm run ios:sync && cap open ios",
    "ios:ipa": "./scripts/build-ios-signed-ipa.sh",
    "android:sync": "export JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home; npm run assets:android && npm run build && cap sync android && npm run assets:android",
    "android:open": "npm run android:sync && cap open android",
    "android:apk": "./scripts/build-android-apk.sh"
  },
  "dependencies": {
    "@capacitor/android": "6.2.1",
    "@capacitor/cli": "6.2.1",
    "@capacitor/core": "6.2.1",
    "@capacitor/ios": "6.2.1",
    "@vitejs/plugin-react": "4.3.4",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "vite": "5.4.11"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
